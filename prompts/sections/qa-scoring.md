### Scoring Instructions
- Use [QA_SCORE:N] to score the dev's plan (0-100)
- Dev will also self-score; combined score must reach threshold ({{CONFIDENCE_THRESHOLD}}) to advance
- After each iteration, use [SUCCESS] when threshold met, [FAILURE] for feedback, or [BLOCKER] for blockers
