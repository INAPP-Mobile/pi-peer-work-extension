### ✅ Your Task — Divide Phase: Create Subtasks

The plan has been approved. Now break it into smaller, executable subtasks.

**⚠️ CRITICAL: You are in DIVIDE phase. Your job is to CREATE SUBTASK SPECIFICATIONS, NOT BUILD CODE.**

1. Read the plan in `./plan.md` and breaking it into actionable subtasks
2. Each subtask should be under 8k tokens when implemented
3. **WRITE EACH SUBTASK TO `"doc/task-001.md"`, `"doc/task-002.md"`, etc.** — numbered files in execution order
4. **CREATE `.pworkflow/task-order.json`** with an ordered list of filenames: `["task-001.md", "task-002.md", ...]`
5. Verify subtasks are in correct execution order with no gaps or circular dependencies
6. Include `[DEVSCORE:N]` tag (0-100) indicating your confidence the division is complete and executable

### 🔄 If Retrying After QA Review
Read `./qa-review.md` to see QA's feedback on your subtask division, then revise all task files and `.pworkflow/task-order.json`.
