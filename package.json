{
  "name": "@earendil-works/pi-peer-workflow",
  "version": "1.0.0",
  "type": "module",
  "description": "Peer-to-peer async workflow pipeline (build → qa → release) for pi agent",
  "keywords": [
    "pi-package",
    "extension",
    "workflow",
    "pipeline"
  ],
  "license": "MIT",
  "files": [
    "index.ts",
    "lib",
    "manifest.json",
    "prompts",
    "scripts"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/INAPP-Mobile/pi-peer-work-extension.git"
  },
  "pi": {
    "extensions": [
      "./lib/index.ts"
    ]
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "node --test test/*.test.js"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "dependencies": {
    "async-mutex": "^0.5.0",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@earendil-works/pi-tui": "^0.79.1",
    "@types/node": "^25.9.2",
    "@earendil-works/pi-ai": "*"
  }
}
